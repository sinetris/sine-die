/*
  Create a file `/assets/css/override_assets.css.gtpl` in your project
  if you need to make changes to the CSS assets.
  (e.g. you need hugo templating to read assets like we do in `assets.css.gtpl`).
*/
